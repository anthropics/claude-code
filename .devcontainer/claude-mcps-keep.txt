# MCP server names to keep from ECC's mcp-configs/mcp-servers.json.
# Others are filtered out by setup-project.sh using jq.
# One name per line; # comments ignored.
#
# NOTE: This produces a *curated reference* at ~/.claude/mcp-configs/mcp-servers.json.
# To actually start a server, register it via `claude mcp add ...` (user scope).
# The file itself does not activate MCP servers — it's a template to copy from.

# Recommended
context7
github
sequential-thinking
memory

# Optional
playwright
exa-web-search
token-optimizer
vercel
