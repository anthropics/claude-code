// Configuration index for enterprise
// This file provides standardized imports for configuration

const config = require('./config.json');
const workflow = require('./workflow.json');

module.exports = {
  config,
  workflow,
};
