# Example env fragment (reference only)
# Real values belong in <repo>/.env (git-ignored), loaded by config/env.
# Keep this file only to remember variable names.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GH_TOKEN=
OPENAI_MODEL=o4-mini
CLAUDE_MODEL=claude-3.7-sonnet
