import type { NudgeOutcome } from './types'

/**
 * Every nudge outcome, the closed list its row's `outcome` is chosen from.
 */
export const NUDGE_OUTCOMES: readonly NudgeOutcome[] = ['shown', 'acted']
