/**
 * What happened to the nudge: `shown`, the toast went up in a session under
 * `claude`; `acted`, a later load ran under another mode after one was shown.
 */
export type NudgeOutcome = 'shown' | 'acted'
