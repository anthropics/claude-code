/**
 * The one line shown at session start under `claude` when the project has
 * an AGENTS.md the engine is not reading and no CLAUDE.md it is.
 */
export const NUDGE =
  'This project has AGENTS.md but no CLAUDE.md; set the agents-md ' +
  "plugin's projectInstructions option to agents-fallback to load it."
