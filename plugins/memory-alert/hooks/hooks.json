{
  "description": "Checks Claude Code process memory after each tool call and alerts when threshold is exceeded",
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/memory-alert.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
