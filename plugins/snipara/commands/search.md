---
description: Search Snipara documentation with regex pattern matching
---

# Search Documentation

Search Snipara docs for pattern: "$ARGUMENTS"

Use `rlm_search` with:
- `pattern`: "$ARGUMENTS"
- `max_results`: 20

Return matching sections with file paths and line numbers.
