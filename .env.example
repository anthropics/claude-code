# ─────────────────────────────────────────────────────────────────────
# Environment Variables for Neon + Vercel GitHub Actions Workflows
#
# These values must be configured in your GitHub repository:
#   Settings → Secrets and variables → Actions
#
# Secrets (encrypted):
#   NEON_API_KEY, VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID
#
# Variables (plaintext):
#   NEON_PROJECT_ID
# ─────────────────────────────────────────────────────────────────────

# ── Neon ──────────────────────────────────────────────────────────
# API key: https://neon.tech/docs/manage/api-keys
NEON_API_KEY=neon_api_key_here

# Project ID: found in Neon Console → Settings
# Configure as a GitHub Actions **variable** (not secret)
NEON_PROJECT_ID=project_id_here

# Connection strings (set automatically by the preview workflow)
DATABASE_URL=postgresql://user:pass@host/dbname
DATABASE_URL_POOLED=postgresql://user:pass@pooler-host/dbname

# ── Vercel ────────────────────────────────────────────────────────
# Token: https://vercel.com/account/tokens
VERCEL_TOKEN=vercel_token_here

# Org & Project IDs: run `vercel link` locally, then check .vercel/project.json
VERCEL_ORG_ID=org_id_here
VERCEL_PROJECT_ID=prj_id_here
