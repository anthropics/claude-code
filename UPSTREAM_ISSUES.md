# Upstream Issue Snapshot

- Upstream parent repository: `anthropics/claude-code`
- Current fork origin repository: `shreyashjagtap157/claude-code`
- Issues have now been enabled for the local fork repository.
- Upstream issue data can be fetched cleanly using the new script `scripts/fetch_upstream_issues.py`.

## Issue sync workflow

- `scripts/fetch_upstream_issues.py` — primary script to pull upstream issues from `anthropics/claude-code` and write `UPSTREAM_ISSUES.json`.
- `UPSTREAM_ISSUES.json` — consolidated issue array output generated by `python scripts/fetch_upstream_issues.py`.

This repository now includes a robust upstream issue sync workflow that normalizes paginated GitHub API output and flattens issue objects for later review.

## Known upstream issue context

- Parent repo open issues count: `9041`
- Issue entries include full GitHub issue object fields such as:
  - `number`
  - `title`
  - `state`
  - `created_at`
  - `updated_at`
  - `closed_at`
  - `user`
  - `labels`
  - `assignees`
  - `comments`
  - `body`
  - `reactions`
  - `timeline_url`
  - `events_url`

## Notes

- The raw data file is comprehensive and can be programmatically flattened or filtered for downstream use.
- If a cleaner flattened JSON array is required, the raw paginated dataset can be transformed into a single issue array.
- This snapshot is intended for later review, triage, and reference of upstream issue details.
